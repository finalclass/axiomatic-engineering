# Infrastructure

[smoketest]

## Deploy script

Add a deploy.sh script that deploys to the testing server.

## Test server

The test server is available at todo.szwalnia.finalizator.pl.

To access the test server use `ssh fcmain`.

The test server runs Caddy.
